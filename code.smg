# Fibonacci

define fib(x) {
    if (x < 0) {
        return 0
    }

    let a = 0
    let b = 1
    while (x > 0) {
        let temp = a + b
        a = b
        b = temp
        x = x - 1
    }
    return b
}

fib(-1) == 0